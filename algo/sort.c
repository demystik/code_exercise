#include <stdio.h>
#include <stdlib.h>
#include <time.h>
int main()
{
int arr[argc];
int num;
time_t t1;
srand((unsigned)time(&t1));
num = rand() % 100;

printf("randomNum is %d", num);

return (0);
}
