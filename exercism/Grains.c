#include <stdio.h>

int main(void)
{
    int i, j;
    i = 1;
    while (i != 65)
    {
        if (i == 1)
            j = 1;
        else
        {
            k += (j + j);
        }
        j++;
        i++;
    }
}